/* exported head */
// I need to define a function named head, which has one parameter 'array'.
// I then need to define a variable named results and assign it an empty array.
// I will use a for loop to check the property in the parameter.
// In the loop, I will push the the values to the empty array.
// When the loop is complete, I will return the values array.
function head(array) {
  return array[0];
}
