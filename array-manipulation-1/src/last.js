/* exported last */
// 1. I need to define the function and label it last with one parameter set to array.
// 2. I will then set a variable results equalling an array literal set to 0.
// 3. I then will return the variable results.
function last(array) {
  return array[array.length - 1];
}

// 1. I needed to get rid of my variable and return the array and in a array
// I set the parameter array and gave it the method length and subtracted 1.
