/* exported drop */
function drop(array, count) {
  array = array.slice(count);
  return array;
}

//  1st draft
//  define function named 'drop' taking 2 parameters 'array' and 'count'
//  we will drop the array at count value using the slice method for the array
//  slice value will be the count value
//  return the new array

//  2nd draft
//  define function named 'drop' taking 2 parameters 'array' and 'count'
//  using slice method on the array taking argument 'count' which is a number
//  assign that value to the 'array' variable
//  return array
